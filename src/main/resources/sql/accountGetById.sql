SELECT
	id,
    accountTypeId,
    username, 
    password, 
    firstName, 
    lastName,
    dob,
    address,
    emailAddress,
    phoneNumber,
    pin,
    bankAccountNumber,
    bankCardNumber,
    createdDate
FROM 
	/*=SCHEMA*/ACCOUNTS 
WHERE 
	ID = :id